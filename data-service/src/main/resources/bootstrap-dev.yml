spring:
  cloud:
    nacos:
      config:
        ext-config:
          - data-id: dev-common.yml
            group: demo
            refresh: true
          - data-id: dev-data.yml
            group: demo
            refresh: true