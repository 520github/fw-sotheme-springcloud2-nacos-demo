
server:
  port: ${port:9092}
  #servlet:
    #context-path: /user

spring:
  application:
    name: userService
  profiles:
    active: ${activeProfile:dev}
  cloud:
    nacos:
      config:
        server-addr: 192.168.5.25:8848
        namespace: demo